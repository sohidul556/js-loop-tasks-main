

/***

Subtask-1:

Display sum of all the odd numbers from 81 to 131.
৮১ থেকে ১৩১ পর্যন্ত সকল বিজোড় সংখ্যার যোগফল প্রদর্শন করুন।

 */

/*
let sum = 0;

for (let i = 81; i <= 131; i++) {
    if (i % 2 !== 0) { // Check if the number is odd
        sum += i; // Add the odd number to the sum
    }
}

console.log(`The sum of all odd numbers from 81 to 131 is: ${sum}`);

*/

/***

Subtask-2:

Display sum of all the even numbers from 206 to 311.
২০৬ থেকে ৩১১ পর্যন্ত সকল জোড় সংখ্যার যোগফল প্রদর্শন করো।

 */

/*
let sum = 0;

for (let i = 206; i <= 311; i++) {
    if (i % 2 === 0) { // Check if the number is even
        sum += i; // Add the even number to the sum
    }
}

console.log(`The sum of all even numbers from 206 to 311 is: ${sum}`);

*/


let sum = 10;
console.log(`The total sum is ${sum}.`);

// ${sum} , dynamically inserts the value of sum into the string.





